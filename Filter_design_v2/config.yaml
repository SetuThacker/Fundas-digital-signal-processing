system:
  output_dir: "./output_plots"

signal_generation:
  fs: 100000.0
  duration: 0.2
  frequencies: [45, 50, 55]  # E.g., 10 Hz signal, 50 Hz noise to notch, 120 Hz noise

notch_experiment:
  target_freq: 50.0  # The exact frequency to eliminate
  pole_radius: 0.95  # The 'r' value for the compensating poles