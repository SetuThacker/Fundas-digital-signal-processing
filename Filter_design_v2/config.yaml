system:
  output_dir: "./output_plots"

multi_root_experiment:
  impulse_length: 50
  zeros: [1.0, -1.0]          # Rejects DC (0 Hz) and Nyquist frequencies
  poles: ["0+0.9j", "0-0.9j"] # Resonates at 1/4 the sampling frequency